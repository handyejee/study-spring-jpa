spring:
  datasource:
    url: jdbc:mysql://localhost:3311/bookdb
    username: testuser
    password: testpw
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true